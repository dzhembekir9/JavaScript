function solve(arr) {
    let first = Number(arr.shift());
    let last = Number(arr.pop());
    console.log(first + last);

    let nums = [1, 2, 3, 4, 5, 6, 7, 8, 9];
 
}

solve(['20', '30', '40']);